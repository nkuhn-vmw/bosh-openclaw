{
  "port": <%= p('openclaw.admin_dashboard.port') %>,
  "broker_endpoint": "<%= p('openclaw.admin_dashboard.broker_endpoint') %>",
  "auth": {
    "username": "<%= p('openclaw.admin_dashboard.auth.username') %>",
    "password": "<%= p('openclaw.admin_dashboard.auth.password') %>"
  },
  "route": {
    "hostname": "<%= p('openclaw.admin_dashboard.route.hostname') %>",
    "domain": "<%= p('openclaw.admin_dashboard.route.domain') %>"
  }
}
